{% set name = "plumpy" %}
{% set version = "0.18.5" %}
{% set sha256 = "a17ad6e00b06d9c54ee76e1267fb51f49a5fd8148fd5ce524b735803527153c0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - aio-pika ~=6.6
    - aiocontextvars ~=0.2.2  # [py < 37]
    - pyyaml ~=5.1.2
    - nest-asyncio ~=1.4.0
    - kiwipy[rmq] ~=0.7.1

test:
  imports:
    - plumpy
    - plumpy.base

about:
  home: https://github.com/aiidateam/plumpy
  license: MIT
  license_file: LICENSE
  summary: A python workflow library that supports multithreading, and RabbitMQ as a message broker for starting/stopping/monitoring tasks.
  dev_url: https://github.com/aiidateam/plumpy

extra:
  recipe-maintainers:
    - ltalirz
    - muhrin
    - sphuber
    - chrisjsewell
    - unkcpz
